namespace Orbsmash.Game
{
    public class GameSettings
    {
        public readonly int NumPlayers = 1;

        public GameSettings(int numPlayers)
        {
            NumPlayers = numPlayers;
        }
    }
}